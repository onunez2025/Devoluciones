node_modules
dist
.env
*.log
.DS_Store
.vscode
build

# Configuracion local de agentes de IA (Claude Code, Gemini/Antigravity).
# Es de cada maquina, no del proyecto.
.claude/
.gemini/
skills-lock.json

# Cache incremental de TypeScript: se regenera en cada `tsc -b`.
*.tsbuildinfo
